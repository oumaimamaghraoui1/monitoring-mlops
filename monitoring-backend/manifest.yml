applications:
  - name: monitoring-backend
    memory: 128M
    buildpacks:
      - nodejs_buildpack
    command: node server.js
    path: .
    env:
      NODE_ENV: production
      NODE_OPTIONS: "--max-old-space-size=128"